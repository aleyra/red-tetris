{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint '*.{js,jsx}'",
    "lint:fix": "eslint --fix '*.{js,jsx}'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
